import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        flex: 1,
        paddingTop: 10,
        alignItems: "center",
    },
    title:{
        fontSize: 20,
        fontWeight: '500',
        color: '#212121'
    }
});

export default styles;